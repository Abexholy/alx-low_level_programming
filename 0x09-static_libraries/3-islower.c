#include "main.h"

/**
 * _islower - used to check for lowercase characters
 * @c: selectyed character to check
 * Return: 1 if c is lowercase, 0 otherwise
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}

