#include "main.h"

/**
 * _isdigit - checks for numbers in a digit (0 through 9)
 * @c: int to be checked through
 * Return: 1 if c is a digit, 0 if otherwise or not
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}

