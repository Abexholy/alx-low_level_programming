#include "main.h"
/**
* sub - subtract two integers
*
* @a: the first integer
* @b: the second integer
*
* Return: subtraction of the two integers
*/
int sub(int a, int b)
{
int ans;
ans = a - b;
return (ans);
}
