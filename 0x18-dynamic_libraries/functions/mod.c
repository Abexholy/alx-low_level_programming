#include "main.h"
/**
* mod - adds two given integers
*
* @a: the first integer
* @b: the second integer
*
* Return: sum of the two integers
*/
int mod(int a, int b)
{
int mod;
mod = a % b;
return (mod);
}
